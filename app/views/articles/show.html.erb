<p id="notice"><%= notice %></p>
<div class="container">
  <p>
      <strong>Title:</strong>
      <%= @article.title %>
  </p>
  <div class="card mb-4">
    <%= @article.content%>  
  </div>
</div>
<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>
